[package]
name = "g5-admin-ssh"
version = "0.1.0"
edition = "2021"
license = "LicenseRef-Proprietary"

[dependencies]
russh = { version = "0.62.2", default-features = false, features = ["flate2", "ring", "rsa"] }
russh-sftp = "2.3.0"
thiserror = "2"
tokio = { version = "1", features = ["fs", "io-util", "sync", "time"] }
