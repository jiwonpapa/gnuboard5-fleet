[package]
name = "g5-admin-security-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
getrandom = "0.4"
thiserror = "2"
totp-rs = { version = "5.7.2", features = ["gen_secret", "otpauth"] }
